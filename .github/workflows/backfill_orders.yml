# ============================================================================
# backfill_orders.yml ─ 大盤委買委賣兩年回補（八片平行）＋ 假說驗證
#
#   ⚠️ 放哪：**StaticWebPage** repo 的 .github/workflows/backfill_orders.yml
#   搭配：order_stats.py 放 CustomerServiceANDSales 根目錄（engines.py 旁邊）
#   怎麼跑：GitHub → StaticWebPage → Actions →「回補大盤委買委賣」→ Run workflow
#
#   形狀照抄 backfill_bars.yml（雷區：TWSE 懸崖綁出口 IP，八片各 ~65 發都在懸崖以下；
#   不要把片數調小）。八片不 push、各自上傳 artifact，merge 只 commit 一次，
#   而且跟 daily.yml 同一個 concurrency group，不會對撞。
#
#   可重複執行：已經在 data/orders_hist.json 的日子保留，撞牆的片下次接著補。
#   merge 最後一步印出「委賣遠超委買是不是低點」的驗證表 —— 那段 log 貼回來。
# ============================================================================
name: 回補大盤委買委賣

on:
  workflow_dispatch:
    inputs:
      years:
        description: "往回抓幾年"
        required: false
        default: "2"

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3, 4, 5, 6, 7]
    steps:
      - name: checkout 程式 repo
        uses: actions/checkout@v4
        with:
          repository: jesselauristonlivermore1877-arch/CustomerServiceANDSales
          token: ${{ secrets.CODE_REPO_TOKEN }}
          path: code

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install requests feedparser certifi

      - name: 抓片 ${{ matrix.shard }}
        working-directory: code
        run: |
          python order_stats.py \
            --shard ${{ matrix.shard }} --of 8 \
            --years ${{ inputs.years }} \
            --out ../out

      - uses: actions/upload-artifact@v4
        with:
          name: orders-${{ matrix.shard }}
          path: out/
          if-no-files-found: warn
          retention-days: 1

  merge:
    needs: crawl
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: daily-build
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4

      - name: checkout 程式 repo
        uses: actions/checkout@v4
        with:
          repository: jesselauristonlivermore1877-arch/CustomerServiceANDSales
          token: ${{ secrets.CODE_REPO_TOKEN }}
          path: code

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install requests feedparser certifi

      - uses: actions/download-artifact@v4
        with:
          path: dl
          pattern: orders-*
          merge-multiple: true

      - name: 合併進 data/orders_hist.json
        working-directory: code
        run: python order_stats.py --merge ../data ../dl

      - name: 驗證「委賣遠超委買＝低點」（這段 log 貼回來）
        working-directory: code
        continue-on-error: true
        run: python order_stats.py --study ../data

      - name: commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/orders_hist.json
          if git diff --staged --quiet; then
            echo "沒有新增任何一天，不提交。"
          else
            git commit -m "data: 回補大盤委買委賣 $(TZ=Asia/Taipei date +%F)"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
